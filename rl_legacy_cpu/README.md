# Superseded CPU copies (do not extend)

Early mirrors of the GEM-side CPU adapters. Training now runs on
`warp_backend/` (Warp physics + batched torch actors, GEM-validated).
Kept for reference only; frozen GEM sources remain authoritative.
